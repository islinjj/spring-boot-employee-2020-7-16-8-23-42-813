pipeline {
    
    agent any

    environment {
        JENKINS_NODE_COOKIE = "donotkillme"
    }

    stages {
        stage('Build') { 
            steps {
                bat "copy \ d:\\deploy\\backend-test"
                bat "cd d:\\deploy\\backend-test"
                echo 'cd d:\\deploy\\backend-test'
                bat 'gradle clean build -x test' 
            }
        }
        stage('deploy') {
            steps {
                echo 'deploy'
                bat "run.bat"
            }
        }
    }
}