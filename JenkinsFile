pipeline {
    
    agent any

    environment {
        JENKINS_NODE_COOKIE = "donotkillme"
    }

    stages {
        stage('Build') { 
            steps {
                bat 'gradle clean build -x test' 
            }
        }
        stage('deploy') {
            steps {
                echo 'deploy'
                bat "copy build\\libs\\*.jar d:\\deploy\\backend-test"
                bat "run.bat"
            }
        }
    }
}